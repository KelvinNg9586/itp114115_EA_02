<div class="container">
  <table class=""table">
  <thead>
    <tr>
        <th>Name</th>
        <th></th>
    </tr>
  </thead>
  <tbody>
  {% for product in products %}
        <tr>
            <td>{{product.title}}</td>
            <td>
                <a href ="{{ url_for('edit_product', product_id=product.id) }}">Edit</a>
            </td>
            <td>
                <a href ="{{ url_for('delete_product', product_id=product.id) }}">Delete</a>
            </td>
        </tr>
   {% endfor %}
   </tbody>
    </table>
    <a href = "{{ url_for('add_product')}}" class="btn btn-primaty">Add_product</a>
</div>
